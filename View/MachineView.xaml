<Window x:Class="WPF_MES_Monitoring_System.View.MachineView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_MES_Monitoring_System.View"
        xmlns:vm="clr-namespace:WPF_MES_Monitoring_System.ViewModel"
        mc:Ignorable="d"
        Title="MachineView" Height="450" Width="800">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <UniformGrid Grid.Row="0" Columns="3" Margin="10">
            <Border Background="#F5F5F5" Margin="5" CornerRadius="8" BorderBrush="#DDD" BorderThickness="1">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="관리 설비" HorizontalAlignment="Center" Foreground="#555" FontSize="14"/>
                    <TextBlock Text="{Binding TotalCount, StringFormat={}{0}대}" HorizontalAlignment="Center" FontSize="28" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="#E8F5E9" Margin="5" CornerRadius="8" BorderBrush="#A5D6A7" BorderThickness="1">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="정상 가동" HorizontalAlignment="Center" Foreground="#2E7D32" FontSize="14"/>
                    <TextBlock Text="{Binding RunningCount}" HorizontalAlignment="Center" FontSize="28" FontWeight="Bold" Foreground="#2E7D32" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="#FFEBEE" Margin="5" CornerRadius="8" BorderBrush="#EF9A9A" BorderThickness="1">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="에러 발생" HorizontalAlignment="Center" Foreground="#C62828" FontSize="14"/>
                    <TextBlock Text="{Binding ErrorCount}" HorizontalAlignment="Center" FontSize="28" FontWeight="Bold" Foreground="#C62828" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </UniformGrid>
        <Grid Grid.Row="1" Margin="10,0,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="설비 필터 : " VerticalAlignment="Center" Margin="0,0,10,0" />
                <ComboBox Width="150" ItemsSource="{Binding MachineOptions}" SelectedItem="{Binding SelectedMachine}" />
            </StackPanel>
            <DataGrid Grid.Row="1" ItemsSource="{Binding LogView}" AutoGenerateColumns="False" ColumnWidth="*" SelectionChanged="DataGrid_SelectionChanged">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Status}" Value="ERROR">
                                <Setter Property="Background" Value="RED"/>
                                <Setter Property="Foreground" Value="White"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    </Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="HorizontalAlignment" Value="Center"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="시간" Binding="{Binding Timestamp, StringFormat='HH:mm:ss'}"/>
                    <DataGridTextColumn Header="설비" Binding="{Binding MachineName}"/>
                    <DataGridTextColumn Header="상태" Binding="{Binding Status}"/>
                    <DataGridTextColumn Header="온도" Binding="{Binding Temperature}"/>
                    <DataGridTextColumn Header="압력" Binding="{Binding Pressure}"/>
                    <DataGridTextColumn Header="메시지" Binding="{Binding LogMessage}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>
